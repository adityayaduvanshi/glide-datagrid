import { Route, Routes, Navigate } from 'react-router-dom';
import Table from './components/data-grid/table';

function App() {
  return (
    <div>
      <Table />
    </div>
  );
}

export default App;
